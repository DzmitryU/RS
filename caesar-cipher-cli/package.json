{
  "name": "caesar-cipher-cli",
  "version": "1.0.0",
  "description": "CLI tool that encodes and decode a text by Caesar cipher",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "dzmitryu",
  "license": "ISC",
  "dependencies": {
    "commander": "^6.1.0"
  }
}
